services:
  ssf-forwarder:
    image: ghcr.io/twosense/ssf-forwarder:latest
    ports:
      - "8080:8080"
    volumes:
      - ./config.yaml:/etc/ssf-forwarder/config.yaml:ro
    restart: unless-stopped
